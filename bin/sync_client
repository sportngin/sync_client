#!/usr/bin/env ruby
require 'rubygems'
require 'bundler'
Bundler.setup
require 'daemons'
require 'sync_client'

Daemons.run_proc('sync_client', :dir_mode => :script, :dir => 'tmp/pids') do
  SyncClient::Poller.run
end
